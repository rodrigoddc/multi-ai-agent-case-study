apiVersion: v1
kind: Service
metadata:
  name: langfuse-web
  namespace: multi-agents
  labels:
    app.kubernetes.io/name: langfuse
    app.kubernetes.io/component: web
spec:
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: 3000
      name: http
  selector:
    app.kubernetes.io/name: langfuse
    app.kubernetes.io/component: web

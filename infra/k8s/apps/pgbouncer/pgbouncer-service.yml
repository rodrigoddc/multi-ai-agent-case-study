apiVersion: v1
kind: Service
metadata:
  name: pgbouncer-svc
  namespace: multi-agents
spec:
  type: ClusterIP
  selector:
    app: pgbouncer
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
